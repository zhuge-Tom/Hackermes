# 下一阶段开发目标

> 更新：2026-08-27。Agent 运行时与授权评估助手能力已在 `0.11.0` 收口。
> 下列项不再适合作为运行时补丁，按独立立项推进。

## 1. 隔离子代理与后台任务

完整子代理编排（deepseek-harness 的 subagent providers / `job_*`）需要与 ToolHost
隔离执行模型和授权范围深度整合：独立进程、独立权限范围、可取消的后台作业、
结果回写主会话。当前 `agent_subtask` 仍是同进程、同策略门、最多 8 轮的有界嵌套。

交付标准：

- 子任务不能提升权限，不能绕过策略门与审批。
- 后台作业可查询 / 取消，主会话崩溃后可恢复状态。
- 有入口点测试锁定隔离边界。

## 2. Linux 转正

Linux x64 仍是预览平台。需要真实桌面 WebKitGTK 全链路验收：捕获 / 拦截 / Fulfill /
视觉矩阵，以及安全工具运行环境（Python 隔离、路径差异、DPAPI 替代实现）。

交付标准：

- 真实 Linux GUI 上复跑 loopback 5/5 与授权评估工作区。
- 发布附件重新包含已验收的 Linux 包。

## 3. 发布门禁与出包卫生

`0.11.0` 以 Release 全量测试与 Windows 自包含打包为发布基线。整套
`invoke-release-acceptance.ps1`（含响应式视觉矩阵）应在下一次对外宣称
“完整发布验收”前对当前源码重跑。

交付标准：

- 门禁 5/5 + 全量测试通过，证据目录归档。
- README 不再用测试数替代视觉/打包门禁事实。

## 4. 评估作业异步化

`assessment_authorize_and_run` / `assessment_run` 仍会等到作业结束（超时已对齐 600s）。
长 ToolHost 适配器更适合“立即返回 jobId + `assessment_job_status` 轮询”。

交付标准：

- 危险长作业默认可异步；取消路径与人工工作台一致。
- 现有同步调用保持兼容或有明确迁移。

## 5. 会话压缩块默认真持久化

ACP 压缩块默认随进程存活；`ai.sessionEvents` 仍是 opt-in。长调查跨重启会丢归档块。
需要在不把工具原文密钥写入默认记忆的前提下，提供可恢复的压缩块持久化。

交付标准：

- 关闭会话事件时也能恢复压缩块引用与摘要。
- 工具参数/原文仍默认不进 `agent-memory.json`。
