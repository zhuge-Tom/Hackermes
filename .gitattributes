# 默认按文本处理并规范化换行
* text=auto

# 源文件一律 UTF-8。中文注释一旦被非 UTF-8 编码写回就会永久损坏,不可逆。
#
# 注意:这里不设 working-tree-encoding —— 那个属性是给"工作区需要非 UTF-8 编码"
# 的场景用的,我们的文件本来就是 UTF-8,设了反而会触发多余的编码转换。
# 真正的风险来自编辑工具:用 PowerShell 的 Set-Content 改含中文的文件会按
# 系统 ANSI 码页写出乱码,改这类文件要用明确以 UTF-8 写入的工具。
*.cs        text eol=crlf
*.axaml     text eol=crlf
*.csproj    text eol=crlf
*.props     text eol=crlf
*.slnx      text eol=crlf

# 前端源码用 LF
*.ts        text eol=lf
*.js        text eol=lf
*.mjs       text eol=lf
*.vue       text eol=lf
*.json      text eol=lf
*.md        text eol=lf

# 二进制
*.ttf       binary
*.png       binary
*.ico       binary
*.mgc       binary
